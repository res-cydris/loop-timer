name: loop_timer
description: A beautiful, customizable repeatable timer app — fully offline.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # State management
  provider: ^6.1.1

  # Local storage
  shared_preferences: ^2.2.2

  # Audio
  audioplayers: ^5.2.1

  # Unique IDs
  uuid: ^4.3.3

  # Animations
  flutter_animate: ^4.5.0

  # Typography
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/sounds/
